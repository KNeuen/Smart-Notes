<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Other styles can be defined here as well -->

    <!-- Style for text alignment buttons -->
    <style name="TextAlignmentButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">4dp</item>
        <!-- Add other properties like padding, text color, etc. -->
    </style>

    <!-- Style for text style buttons -->
    <style name="TextStyleButton" parent="TextAlignmentButton">
        <!-- Inherits properties from TextAlignmentButton -->
        <!-- Add or override specific properties for text style buttons -->
    </style>
</resources>
